/**
 * Demo AgentMail inbox used by `/dance-extras` bot-action and hub “Extra” bot flow.
 * Server still needs `AGENTMAIL_API_KEY` for Bearer sends; `inbox_id` selects the sending inbox.
 */
export const AGENTMAIL_DEMO_INBOX_ID = 'streetkode@agentmail.to'
